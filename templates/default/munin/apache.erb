[apache_processes]
env.url http://localhost:%d/server-status?auto
env.port <%= node['apache']['listen_ports'].first %>
 
[apache_accesses]
env.url http://localhost:%d/server-status?auto
env.port <%= node['apache']['listen_ports'].first %>
 
[apache_volume]
env.url http://localhost:%d/server-status?auto
env.port <%= node['apache']['listen_ports'].first %>
